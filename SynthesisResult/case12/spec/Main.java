import java.util.*;
import java.time.LocalTime;

public class A {
    public void F(String[] args) {
        String a = "a";
        String b = "a";
        Integer i = new Integer(1);
        Integer j = new Integer(2);

        //Target
        assert(a == b);

        //Target
        assert(b == a);
        assert(c.equals(a + b));
    }
}
